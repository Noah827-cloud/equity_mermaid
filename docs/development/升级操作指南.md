# 股权结构图工具升级操作指南

## 版本
创建时间：2024-01-20

## 升级前准备

1. **检查当前运行的应用进程**
   ```powershell
   tasklist | Select-String -Pattern "equity"
   ```

2. **终止旧版本应用进程**
   ```powershell
   taskkill /F /PID [进程ID1] /PID [进程ID2]
   ```
   （使用实际的进程ID替换[进程ID1]和[进程ID2]）

## 源码修改（如需）

1. **检查并修改源代码**
   - 打开源代码文件：`src/main/enhanced_equity_to_mermaid.py`
   - 找到测试数据路径相关代码进行修改
   - 同时更新相关的错误提示信息

## 创建补丁包

1. **生成补丁包**
   ```powershell
   cd c:\Users\z001syzk\Downloads\equity_mermaid
   .\build_exe_incremental.bat patch [新版本号]
   ```
   （例如：`.\build_exe_incremental.bat patch 1.04`）当前最新版本是1.04。

2. **获取补丁文件位置**
   - 补丁文件通常生成在：`dist/patches/equity_mermaid_patch_[时间戳]/` 目录下
   - 文件名为：`equity_mermaid_patch_[时间戳].zip`

## 应用补丁

1. **复制补丁文件到基础版本目录**
   ```powershell
   Copy-Item -Path "c:\Users\z001syzk\Downloads\equity_mermaid\dist\patches\equity_mermaid_patch_[时间戳]\equity_mermaid_patch_[时间戳].zip" -Destination "c:\Users\z001syzk\Downloads\equity_mermaid\dist\equity_mermaid_tool_incremental\"  
   ```

2. **更新补丁应用脚本**
   - 编辑：`c:\Users\z001syzk\Downloads\equity_mermaid\dist\equity_mermaid_tool_incremental\apply_patch.ps1`
   - 更新补丁文件名：`$patch_file = 'equity_mermaid_patch_[时间戳].zip'`

3. **运行补丁应用脚本**
   ```powershell
   cd c:\Users\z001syzk\Downloads\equity_mermaid\dist\equity_mermaid_tool_incremental
   powershell -ExecutionPolicy Bypass -File apply_patch.ps1
   ```

## 启动应用

1. **使用启动脚本启动应用**
   ```powershell
   cd c:\Users\z001syzk\Downloads\equity_mermaid\dist\equity_mermaid_tool_incremental
   .\start_app.bat
   ```

## 验证升级

1. **功能验证**
   - 点击"🧪 加载测试数据"按钮
   - 确认系统尝试加载 `translated_test_data.json` 文件
   - 检查是否能正常显示股权结构图

2. **错误处理验证**
   - 如果测试数据文件不存在，确认错误提示正确显示

3. **问题排查**
   - 如果仍然遇到"找不到测试数据文件"的错误：
     - 确保所有相关进程已被终止：
       ```powershell
       tasklist | Select-String -Pattern "equity"
       taskkill /F /PID [进程ID]
       ```
     - 检查打包目录中的文件结构：
       ```powershell
       Get-ChildItem -Path "dist\equity_mermaid_tool_incremental\app\archive\examples_backup_20251001" -Recurse
       ```
     - 确保`translated_test_data.json`文件存在于正确的位置。

## 最近更新

- 2024-01-20: 创建1.04版本补丁，优化路径解析功能，添加更多候选路径来查找测试数据文件
- 2024-01-20: 创建1.02和1.03版本补丁，修复测试数据文件路径问题

## 注意事项

- 确保在应用补丁前终止所有正在运行的应用进程
- 补丁版本号必须符合递增规则
- 基础版本必须与补丁要求的基线版本匹配
- 如果补丁应用失败，可以尝试使用PowerShell脚本手动应用