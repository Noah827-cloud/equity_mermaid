# 核心Web框架
streamlit>=1.32.0
streamlit-mermaid>=0.2.0

# AI和云服务
dashscope>=1.10.0
requests>=2.31.0

# 数据处理
pandas>=2.0.0
openpyxl>=3.1.0
pyarrow>=10.0.0

# 并发文件锁
portalocker>=2.7.0

# 安全和配置
python-dotenv>=1.0.0
cryptography>=42.0.0

# 图像处理（图像识别模式）
pillow>=10.0.0

# 类型提示支持（Python < 3.10）
typing-extensions>=4.5.0

# 注意：
# - visjs相关依赖通过CDN加载，无需额外Python包
# - subprocess、os、json、re等标准库模块已内置，无需安装
# - 已移除未使用的依赖：networkx、matplotlib、xlrd、xlsxwriter